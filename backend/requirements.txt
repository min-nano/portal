fastapi>=0.141.1,<1
uvicorn[standard]>=0.52.1,<1
openpyxl==3.1.5
# 構造計算安全証明書（PDF）の読み書き。pdfminer.six は文字の座標取得（○ を
# 付ける位置の特定と、既存 PDF の解析）に、pypdf は ○ の重ね合わせと文書情報の
# 読み書きに使う。python-multipart は PDF アップロードの受け取りに必要。
pypdf>=6.6.1,<7
pdfminer.six>=20250506
python-multipart>=0.0.20,<1
# 釘配列諸定数の計算は Rust で書いた唯一の実装（core/）を wasm として動かす。
# 画面（ブラウザ）が動かすのとまったく同じ .wasm をサーバも読み込むため、
# 計算の実装が 2 つに分かれない（README「計算の一元管理（Rust → wasm）」参照）。
# wasmtime の wheel はネイティブライブラリ同梱で、ビルドに Rust は要らない。
wasmtime>=47.0.1,<48
# 釘配列諸定数の計算書 PDF は雛形を使わずバックエンドが直接組み立てる。
# fontTools は、同梱した日本語フォント（app/fonts、SIL OFL 1.1）から
# 「その PDF で実際に使った文字だけ」を切り出して埋め込むために使う。
fonttools>=4.62.1,<5
PyJWT[crypto]>=2.13.0,<3
google-auth>=2.56.3,<3
google-cloud-firestore>=2.28.1,<3
requests>=2.34.2,<3
