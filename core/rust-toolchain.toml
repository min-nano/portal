# ビルドに使う toolchain を固定する。成果物（.wasm）はデプロイのたびに CI が
# 作り直すので、新しい stable が出た日に理由もなくビルドが変わったり壊れたり
# しないよう、上げるときは意図して上げる。
#
# 固定してあるおかげで、手元でビルドした .wasm と CI がビルドしたものは
# バイト列まで一致する（この crate は外部クレートに依存していないため）。
[toolchain]
channel = "1.94.1"
targets = ["wasm32-unknown-unknown"]
